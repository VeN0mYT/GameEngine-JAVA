#version 330 core

layout (location = 0) in vec2 aPos;   // position (x, y)
layout (location = 1) in vec3 aColor; // color (r, g, b)

out vec3 vertexColor; // pass to fragment

void main()
{
    gl_Position = vec4(aPos, 0.0, 1.0);
    vertexColor = aColor;
}
